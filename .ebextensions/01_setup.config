commands:
  01_setup_swap:
    command: |
      if [ ! -e /var/swapfile ]; then
        fallocate -l 2G /var/swapfile
        chmod 600 /var/swapfile
        mkswap /var/swapfile
        swapon /var/swapfile
      fi